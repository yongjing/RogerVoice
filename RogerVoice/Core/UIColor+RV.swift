//
//  UIColor+RV.swift
//  RogerVoice
//
//  Created by Yongjing CHEN on 14/11/2024.
//
import Foundation
import UIKit
import SwiftUI

extension UIColor {

    struct RVColor {
        static let greenRogerVoice = UIColor(red: (28.0)/255, green: (202.0)/255, blue: (179.0)/255, alpha: (1.0))
    }
}

extension Color {

    struct RVColor {
        static let green = Color(red: (28.0)/255, green: (202.0)/255, blue: (179.0)/255)
        static let textGreen = Color(red: (2.0)/255, green: (117.0)/255, blue: (99.0)/255)
        static let backgroundGreen = Color(red: (214.0)/255, green: (250.0)/255, blue: (242.0)/255)
        static let lightGray = Color(red: (246.0)/255, green: (248.0)/255, blue: (249.0)/255)
        static let textGray = Color(red: (19.0)/255, green: (56.0)/255, blue: (59.0)/255)
        static let backgroundGray = Color(red: (250.0)/255, green: (251.0)/255, blue: (252.0)/255)
    }
}
